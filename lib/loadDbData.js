const root = process.cwd()const app = require(root + '/app')const datagen = require(root + '/lib/datagen')const data = require(root + '/fixtures/mockDataDevDb')const Log = require('log')    , log = new Log('info');datagen.clear().then(() => {    return datagen.loadData(data)  }).then(() => {    log.info('Loaded Data. Exit')    process.exit(0);  }).catch((err) => {    log.error('Error loading data: ' + new Error(err.stack));    process.exit(-1);  })